server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://postgres-pagamento:5432/pagamentos
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true