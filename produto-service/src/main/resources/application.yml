server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://postgres-produto:5432/produtos
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true